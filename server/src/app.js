// crea la instancia de express
// configura middlewares globales: 
//      - express.json()
//      - express.urlencoded()
// monta los routers:
//      - app.use("/api/products", productsRouter)
//      - app.use("/api/carts", cartsRouter)
// exporta app
// no levanta el servidor, solo configura la app 